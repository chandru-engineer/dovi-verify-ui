version: "3.9"

services:
  web:
    build: .
    container_name: dovi-verify-portal
    ports:
      - "3003:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
